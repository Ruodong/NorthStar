fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
neo4j==5.25.0
httpx==0.27.2
python-multipart==0.0.12
asyncpg==0.30.0
beautifulsoup4==4.12.3
Pillow==11.0.0
defusedxml
boto3>=1.36.0
# Schema version tracking — Alembic. 001_baseline.py snapshots the pre-Alembic
# flat-SQL state (backend/sql/001..018). New schema goes through Alembic from
# 002+. ensure_sql_migrations() still runs flat SQL at startup (idempotent),
# then stamps alembic_version=001_baseline if absent.
alembic>=1.13.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
# psycopg v3 (sync + async) — required by scripts/sync_architecture_templates.py
# when triggered as a FastAPI BackgroundTask from the Settings router.
psycopg[binary]>=3.2
